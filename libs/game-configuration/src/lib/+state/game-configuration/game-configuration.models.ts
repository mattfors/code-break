export interface GameConfiguration {
  secretCode: number[];
  columns: number;
  rows: number;
  numberOfColors: number;
}

export interface GameDefaults {
  columns: number;
  rows: number;
  numberOfColors: number;
}
